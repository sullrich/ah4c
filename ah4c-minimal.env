HOST_DIR=/data

# Intel integrated graphics (and AMD GPUs) are exposed through /dev/dri.
# Change this to /dev/null only if the host has no /dev/dri device.
GPU_DEVICE=/dev/dri

# DOCKER_RUNTIME=nvidia
# NVIDIA_VISIBLE_DEVICES=all
# NVIDIA_DRIVER_CAPABILITIES=compute,utility,video
