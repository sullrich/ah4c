HOST_DIR=/data

# Intel or AMD graphics only: uncomment to expose the host GPU through /dev/dri.
# GPU_DEVICE=/dev/dri

# DOCKER_RUNTIME=nvidia
# NVIDIA_VISIBLE_DEVICES=all
# NVIDIA_DRIVER_CAPABILITIES=compute,utility,video
